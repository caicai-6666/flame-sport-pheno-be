# Agent 工作指引

本文件适用于整个仓库。任何 agent 在分析、设计或修改项目之前，都必须先阅读 [`description/README.md`](description/README.md) 和 [`description/project.md`](description/project.md)，通过文档地图定位资料，并建立对业务流程、领域对象、代码分层和当前阶段限制的基本理解。

## 开始工作前

1. 阅读 `description/README.md` 和 `description/project.md`。
2. 根据其中的“按业务主题阅读”导航，继续阅读本次任务相关的 `business/`、`api/`、`db/` 和 `dev/` 文档。
3. 阅读对应 Router，再沿 `service -> repository -> model` 追踪现有实现。
4. 确认需求影响范围后再开始修改，不根据单个文件孤立推断完整业务逻辑。

如果代码与文档存在冲突，应先指出冲突并确认预期行为，不要自行扩大需求或顺带重构无关模块。

---

## 代码编写规则

- 遵循现有 `router -> service -> repository -> model` 分层和异步调用方式。
- 优先复用已有 Service、Repository 及 `app/core/` 能力，不重复实现相同逻辑。
- Router 只负责 HTTP 参数、依赖注入和调用 Service；跨表校验、状态流转和事务由 Service 处理；Repository 聚焦数据访问。
- 在关键位置添加简洁的中文注释，重点说明不直观的业务规则、事务边界、并发锁、缓存策略、异常补偿和临时兼容逻辑。
- 不为显而易见的赋值或简单调用逐行添加注释。注释需要解释“为什么”，避免只复述代码“做了什么”。
- 修改应保持在需求所需范围内，不擅自引入大型框架、抽象层或无关重构。

---

## 数据库文档规则

`description/db/` 默认视为只读目录。只有满足以下任一条件时，agent 才可以修改其中的文件：

- 当前任务正在明确讨论数据库或表结构设计。
- 用户明确要求新增或修改数据库描述文档。

普通接口开发、业务逻辑调整或代码整理，不得顺带修改 `description/db/`。

数据库描述文档尽量由以下三部分构成：

```text
表介绍
字段介绍
建表语句
```

撰写数据库文档时应遵守：

- “表介绍”简要说明表的职责、保存的数据以及与核心对象的必要关系。
- “字段介绍”说明字段类型、约束、默认值和字段本身的含义。
- “建表语句”提供与设计一致的完整 SQL。
- 不在数据库文档中展开接口流程、页面交互或大量跨模块业务逻辑。
- 只说明理解表职责和字段设计所必需的业务背景，详细流程放入 `description/business/`。

---

## 功能与文档同步

所有新增或修改的 Markdown 内容必须遵循 [`description/document-style.md`](description/document-style.md)。该文件是项目文档结构、格式、表达和维护方式的唯一规范来源。

完成功能开发时，必须在同一任务中及时更新相关文档：

- 新增或修改接口：更新 `description/api/{router}.md`。
- 新增或修改业务规则、状态流转或跨接口流程：更新对应的 `description/business/*.md`。
- 修改运行方式、配置、资源或开发流程：更新对应的 `description/dev/*.md`。
- 明确修改数据库设计：更新对应的 `description/db/*.md`，并确保文档、Model 和实际建表语句一致。
- 项目整体能力、主要流程或文档导航发生变化：更新 `description/project.md`；对外项目概览发生变化时同步更新根目录 `README.md`。

不要把代码改动完成但文档仍描述旧行为的任务视为完成。

---

## 完成前检查

1. 验证实现符合相关业务和 API 文档。
2. 运行与改动范围匹配的测试或检查，并说明无法执行的验证。
3. 检查相关文档是否已同步更新。
4. 确认没有误改 `description/db/` 或其他无关文件。
5. 总结实际修改、验证结果和仍存在的限制。
