# 用户接口

## 接口概览

当前路由提供以下接口。

| 方法 | 路径 | 是否鉴权 | 说明 |
| --- | --- | ---: | --- |
| `GET` | `/flame/api/user` | 否 | 用户子路由存活校验 |

---

## GET `/flame/api/user`

成功响应：

```json
{
  "code": 200
}
```

---

当前不提供用户资料登记或更新接口。用户基础信息由生产模式首次钉钉登录初始化；阶段型挑战所需的健康指标由用户填写在凭证备注中，不保存到用户表。
